<%= form_for(war) do |f| %>
  <% if war.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(war.errors.count, "error") %> prohibited this war from being saved:</h2>

      <ul>
      <% war.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :enemy %>
    <%= f.text_field :enemy %>
  </div>

  <div class="field">
    <%= f.label :our_stars %>
    <%= f.number_field :our_stars %>
  </div>

  <div class="field">
    <%= f.label :enemy_stars %>
    <%= f.number_field :enemy_stars %>
  </div>

  <div class="field">
    <%= f.label :our_descruction %>
    <%= f.number_field :our_descruction %>
  </div>

  <div class="field">
    <%= f.label :enemy_descruction %>
    <%= f.number_field :enemy_descruction %>
  </div>

  <div class="field">
    <%= f.label :end_result %>
    <%= f.check_box :end_result %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
